* count how much data is sent, somehow
* make the script non-dependant on a hardcoded string
  (because of the 'is_authorizedkeys_command')
* cleanup all the comm comparisons functions
* refactor functions
* improve names for helper functions
